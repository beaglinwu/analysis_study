**第一关：数据探索（基础）**

- 查看数据基本信息：形状、字段类型、每列缺失值数量和比例
- 用 `describe()` 看数值型字段的分布
- 回答：哪几列有缺失值？缺失比例分别是多少？

**第二关：数据清洗（核心）**

- `Age` 列缺失约20%，用合适的方式填充（思考：用均值？中位数？还是按性别/舱位分组后填充？说明你的理由）
- `Cabin` 列缺失约77%，决定如何处理，并说明原因
- `Embarked` 列只缺失2个，用最合适的方式填充
- 检查是否有重复行，有的话删除

**第三关：数据分析（进阶）**

- 整体生存率是多少？
- 用 `groupby` 分别计算：男女生存率、三个舱位（Pclass）生存率、不同登船港口生存率
- 新建一列 `AgeGroup`，把年龄分为：儿童(0-12)、青年(13-35)、中年(36-60)、老年(60+)，分析各年龄段生存率

**第四关：可视化（加分）**

- 画一张柱状图：各舱位的生存率对比
- 画一张图：男女在不同舱位下的生存率对比（用 seaborn 的 `barplot`，加 `hue` 参数）
- 画 `Age` 的分布直方图，清洗前后各画一张对比

**第五关：写出你的结论** 用自己的话写3-5条分析结论，格式参考：

> "一等舱乘客生存率为XX%，显著高于三等舱的XX%，说明......"

